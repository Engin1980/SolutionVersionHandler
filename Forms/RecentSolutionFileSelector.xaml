<Window x:Class="SolutionVersionHandler.Forms.RecentSolutionFileSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SolutionVersionHandler.Forms"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
                FontSize="14"
        FontFamily="Bookman Old Style"
        Title="RecentSolutionFileSelector" Height="150" Width="800">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Top row: label + combo -->
    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 8">
      <TextBlock Margin="0 0 8 0" VerticalAlignment="Center" Text="Select a recent solution file:" />
      <ComboBox x:Name="cmbRecentSolutions" Width="400" VerticalAlignment="Center"
                ItemsSource="{Binding RecentSolutionFiles}">
        <ComboBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal">
              <Button x:Name="btnDeleteRecentSolutionFile" Click="btnDeleteRecentSolutionFile_Click" Content="⌦" Margin="0 0 2 0" Padding="4 0 2 0" />
              <StackPanel Orientation="Horizontal" Margin="2">
                <TextBlock Text="{Binding FileName}" />
                <TextBlock Text=" (" Foreground="Gray" />
                <TextBlock Text="{Binding FilePath}" Foreground="Gray" />
                <TextBlock Text=")" Foreground="Gray" />
              </StackPanel>
            </StackPanel>
          </DataTemplate>
        </ComboBox.ItemTemplate>
      </ComboBox>
    </StackPanel>

    <!-- Bottom row: buttons aligned to right -->
    <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
      <Button x:Name="btnOK" Width="75" Margin="0 0 8 0" Padding="6" Click="btnOK_Click">OK</Button>
      <Button x:Name="btnCancel" Width="75" Padding="6" Click="btnCancel_Click">Cancel</Button>
    </StackPanel>
  </Grid>
</Window>
